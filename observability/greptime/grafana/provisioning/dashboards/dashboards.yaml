apiVersion: 1

# The image's own RED dashboards are absent here: they are generated by Tempo's
# metrics_generator, which GreptimeDB has no equivalent for.
providers:
   - name: 'SLM'
     type: file
     options:
        path: /etc/grafana/slm-dashboards
        foldersFromFilesStructure: false
