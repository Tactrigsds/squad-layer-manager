// The whole of rxjs, deliberately: curating operators would be a treadmill with no upside, and rxjs
// carries its own semver. An rxjs upgrade that moves this surface is a plugin API change, which is
// what the report will say.
export * from '@/lib/rxjs'
