/** The read surface. Loading and finalizing match state belongs to the host. */
export { getCurrentMatch, getMatchById, getPublicMatchHistoryState, getRecentMatches } from '@/systems/match-history.server'
