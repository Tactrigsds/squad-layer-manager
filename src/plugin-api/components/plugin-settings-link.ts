/**
 * A link to the plugin's own config, for a panel that needs to say "set this up" without hardcoding the
 * settings route or the anchor scheme.
 */
export { PluginSettingsLink } from '@/components/plugin-settings-link'
