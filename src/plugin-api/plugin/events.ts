/** Rendering a plugin's own app events in the activity feed. */
export { registerEventRenderer as register } from '@/systems/plugins.client'
export type { EventIcon, EventRendering, PluginEventView } from '@/systems/plugins.client'
